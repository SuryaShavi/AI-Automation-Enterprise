server:
  port: 8085

spring:
  application:
    name: document-service

springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info